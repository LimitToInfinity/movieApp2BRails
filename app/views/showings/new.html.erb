


<%= form_for @showing do |f| %>
<%= f.label :time %> 
<%= f.number_field :time %>
<%= f.label :movie %>
<%= f.collection_select :movie_id, Movie.all, :id, :title, prompt: true %>
<%= f.submit %>
<% end %>